export class EntityPropertyTab {
    constructor(base) {
        this.base = base;
        this.setupBindings();
        this.initializeKendoComponents();
    }

    /**
    * Initializes all kendo components for the base class.
    */
    initializeKendoComponents() {
        // todo: move components from entitytab
    }

    async setupBindings() {
        // todo: move bindings from entitytab
    }
}
