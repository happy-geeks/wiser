@model FrontEnd.Modules.Base.Models.BaseModuleViewModel

<!DOCTYPE html>
<html>

<head runat="server">
    <title>Wiser 2.1 - Search</title>

    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- TODO: These files should be loaded via webpack like the rest, but then we get errors.
    TODO: If we add the NPM package @@progress/kendo-theme-material, then it works, but that theme has different CSS, some thing look different then... -->
    <link href="/css/libraries/Kendo/kendo.common-material.min.css" rel="stylesheet" />
    <link href="/css/libraries/Kendo/kendo.material.min.css" rel="stylesheet" />

    <script>
        window.wiserVersion = "@Model.WiserVersion?.ToString()";
    </script>
</head>

<body data-sub-domain="@Model.SubDomain"
      data-wiser-api-root="@Model.ApiRoot"
      data-is-test-environment="@Model.IsTestEnvironment"
      data-wiser-api-v21-root="@Model.ApiRoot"
      data-wiser-api-authentication-url="@Model.ApiAuthenticationUrl"
      data-api-client-id="@Model.Settings.ApiClientId"
      data-api-client-secret="@Model.Settings.ApiClientSecret"
      data-track-js-token="@Model.Settings.TrackJsToken">
    <div id="wiser" class="searching">
        <div class="pane-content">
            <div class="search-container">
                <select id="entityTypeComboBox"></select>
                <select id="propertyComboBox"></select>
                <select id="operatorComboBox"></select>
                <div class="text-container">
                    <input type="text" placeholder="Vul een zoekwaarde in (druk op enter om te zoeken)..." id="search-field" class="k-input" />
                    <div class="icon-line-search"></div>
                    <div class="loader-icon"><div></div><div></div><div></div><div></div></div>
                </div>
                <div class="checkbox-container">
                    <input type="checkbox" id="includingDeletedItems" name="includingDeletedItems" class="hidden" />
                    <label for="includingDeletedItems"><span>Ook verwijderde items zoeken</span></label>
                </div>
            </div>
            <div class="search-output">
                <h3>Zoekresultaten: <span id="resultsCount">0</span> items</h3>

                <div id="search-grid" class="grid"></div>
            </div>
        </div>
    </div>

    <div id="dynamicItemWindow"></div>

    <script src="/scripts/Utils.min.js"></script>
    <script src="/scripts/Search.min.js"></script>
</body>
</html>